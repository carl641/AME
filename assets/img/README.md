Drop the photography named in the root README here.

Each slot in the page carries the path it expects. The script preloads that
path and only swaps in a real `<img>` once it loads, so a missing file shows a
labelled placeholder instead of a broken image.
